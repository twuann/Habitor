<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Notification permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Exact alarm permission for habit reminders -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    
    <!-- Boot completed permission for rescheduling alarms after device restart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Location permissions for habit location feature -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Habitor"
        tools:targetApi="31">

        <!-- Màn hình chính (Navigation Drawer) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true" />

        <!-- Màn hình khởi động (Onboarding) -->
        <activity
            android:name=".activities.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receiver dùng cho AlarmManager để gửi thông báo -->
        <receiver
            android:name=".utils.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <!-- Boot Receiver for rescheduling alarms after device restart -->
        <receiver
            android:name=".utils.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Notification Action Receiver for handling Mark Complete and Snooze actions -->
        <receiver
            android:name=".utils.NotificationActionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.habitor.ACTION_MARK_COMPLETE" />
                <action android:name="com.example.habitor.ACTION_SNOOZE" />
            </intent-filter>
        </receiver>

        <!-- End of Day Reminder Receiver for high priority habits -->
        <receiver
            android:name=".utils.EndOfDayReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Geofence Broadcast Receiver for location-based reminders -->
        <receiver
            android:name=".utils.GeofenceBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.location.Geofence" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
